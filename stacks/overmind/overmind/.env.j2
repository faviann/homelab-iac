# TEMPORARY during active application design: replace the inline development
# agent identity with vault-backed structured agent records when the model stabilizes.
OVERMIND_VERSION={{ stack_vars.overmind_version }}
POSTGRES_ADMIN_PASSWORD={{ stack_vars.postgres_admin_password | compose_env }}
MEMSRV_PASSWORD={{ stack_vars.memsrv_password | compose_env }}
OVERMIND_HTTP_BIND=0.0.0.0
OVERMIND_HTTP_PORT=8080
OVERMIND_AGENT_KEYS_FILE=./agent-keys.yaml
MEMSRV_AGENT_KEYS_PATH=/run/secrets/agent-keys.yaml
