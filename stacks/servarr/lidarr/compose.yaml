x-prereq-dirs:
  - ./appdata/lidarr

services:
  lidarr:
    image: lscr.io/linuxserver/lidarr:latest
    container_name: lidarr
    hostname: lidarr
    restart: unless-stopped
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ./appdata/lidarr:/config
      - /data:/data
    ports:
      - 8686:8686
    labels:
      traefik.enable: true
      homepage.instance.admin.group: Media
      homepage.instance.admin.name: Lidarr
      homepage.instance.admin.href: https://${HOMEPAGE_FQDN}
      homepage.instance.admin.description: Music management
      homepage.instance.admin.icon: lidarr
    networks:
      - servarr-internal

networks:
  servarr-internal:
    name: servarr-internal
    external: true