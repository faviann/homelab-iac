[project]
name = "server-management-scripts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ansible>=13.5.0",
    "ansible-lint>=24.0.0",
    "yamllint>=1.32.0",
    "requests>=1.1",
    "proxmoxer>=2.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false