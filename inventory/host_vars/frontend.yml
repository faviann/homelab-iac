---
# Host-specific configuration for frontend
# Inherits from: tier_small, cap_docker, cap_service_agents

# Container definition for proxmox_lxc_provision role
proxmox_lxc_overrides:
  vmid: 302
  hostname: frontend
  description: "Frontend application service managed via Ansible"
  tags:
    - ansible
    - frontend
    - web
