---
# Host-specific configuration for the reverse proxy
# Inherits from: tier_medium, cap_docker

# Container definition for proxmox_lxc_provision role
proxmox_lxc_overrides:
  vmid: 300
  hostname: gatekeeper
  description: "Reverse proxy service managed via Ansible"
  tags:
    - ansible
    - reverse-proxy
    - web
