---
# Default variables for proxmox_host_bootstrap role
# Prepares SSH access and validates Proxmox host readiness

# Proxmox host connection details
proxmox_host: "{{ proxmox_api_host | default('proxmox.vms') }}"
proxmox_ssh_user: root

# SSH key configuration - references control_node_bootstrap variables for DRY
# These paths should match what control_node_bootstrap generates
# Project-relative paths (all configs in .ansible/ subdirectory)
proxmox_ssh_key_private: "{{ control_node_ssh_private_key_path | default(playbook_dir + '/../.ansible/ssh/proxmox_lxc') }}"
proxmox_ssh_key_public: "{{ control_node_ssh_public_key_path | default(playbook_dir + '/../.ansible/ssh/proxmox_lxc.pub') }}"

# Connection timeouts
proxmox_ssh_connect_timeout: 5
proxmox_ssh_port: 22

# Validation checks
proxmox_validate_pct: true
proxmox_validate_api: true
