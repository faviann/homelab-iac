---
# Main tasks file for proxmox_api_adapter role
# Provides normalized access to Proxmox API responses
# This role is a placeholder for future API abstraction
# Currently, API calls are made directly in playbooks/tasks

# Future enhancements:
# - Centralize all API endpoint definitions
# - Normalize API response structures
# - Handle API versioning
# - Provide reusable API call patterns

- name: API adapter initialized
  ansible.builtin.debug:
    msg: "Proxmox API adapter role loaded (placeholder for future abstraction)"
  run_once: true
