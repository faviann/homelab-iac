---
- name: Ensure controller SSH key is installed in the observed guest
  ansible.builtin.include_role:
    name: infrastructure/lxc_ssh_key_injector
  vars:
    lxc_ssh_key_injector_guest_bootstrap: "{{ proxmox_lxc_contract.guest_bootstrap }}"
