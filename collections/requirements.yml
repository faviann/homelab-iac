---
collections:
  # ansible.posix: Required for mount module. Pin to latest available version.
  # Note: Version 2.1.0 has deprecation warnings for ansible.module_utils._text imports
  # that will be fixed in future releases when the module is updated to use
  # ansible.module_utils.common.text.converters. These warnings are harmless.
  - name: ansible.posix
    version: ">=2.1.0"
  - name: community.proxmox
    version: ">=1.3.0"
  - name: community.crypto
    version: ">=2.16.0"

roles:
  - name: geerlingguy.docker
    version: "7.4.1"
